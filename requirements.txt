# Garmin data access (same library the Taxuspt/garmin_mcp server uses under the hood)
garminconnect>=0.2.25
garth>=0.5.0

# Storage / analysis
pandas>=2.2.0
numpy>=1.26.0

# Dashboard. Charts are hand-built SVG (garmin_tracker/charts.py), so there is
# no plotting-library dependency.
# >=1.49 for the width="stretch" widget API used throughout dashboard.py.
streamlit>=1.49.0

# Config
python-dotenv>=1.0.1

# Tests
pytest>=8.0.0

# Web service (Vercel deployment). Not needed for local CLI/Streamlit use.
fastapi>=0.117.1
python-multipart>=0.0.9
psycopg[binary]>=3.2.0
